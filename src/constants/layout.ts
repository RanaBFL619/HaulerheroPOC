/**
 * Shared page layout classes for consistent spacing on all sides.
 * Use on every page so content has the same left/right and vertical spacing.
 */
export const PAGE_OUTER = "min-h-screen bg-background py-6 md:py-8";
export const PAGE_CONTAINER = "max-w-7xl mx-auto w-full px-6 md:px-8";
